# 백엔드 주소. VITE_ 접두사가 없으므로 클라이언트 번들에 포함되지 않습니다.
# 로컬은 vite dev proxy가, 배포 환경은 Vercel Function(api/proxy.js)이 이 값을 사용합니다.
# 배포 환경 값은 Vercel > Settings > Environment Variables에 등록하세요.
BACKEND_ORIGIN=http://your-backend-host:8080
